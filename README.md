# AuTUI 

## Description 

Building lego-style automations for linux 
Only support simple actions (e.g. cd, mkdir ...) 

## Installation 

Create a build directory

```bash
mkdir -p build && cd build
```

Configure the project
```bash
cmake ..
```

Build the executable
```bash
make
```
